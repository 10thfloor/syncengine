{
  "name": "@syncengine/cli",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "description": "syncengine CLI — dev, start, build, serve, add",
  "type": "module",
  "bin": {
    "syncengine": "./bin/syncengine.mjs"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "compile": "bun build --compile --minify --sourcemap=none --outfile=dist/syncengine src/index.ts",
    "compile:all": "tsx scripts/compile-all.ts"
  },
  "dependencies": {
    "@syncengine/bin-utils": "workspace:*",
    "@syncengine/core": "workspace:^",
    "@syncengine/nats-bin": "workspace:*",
    "@syncengine/restate-bin": "workspace:*",
    "@syncengine/serve-bin": "workspace:*",
    "esbuild": "^0.28.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.9.3",
    "vitest": "^1.0.4"
  }
}
