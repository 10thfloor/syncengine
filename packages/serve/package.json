{
  "name": "@syncengine/serve",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "compile": "bun build --compile --minify --sourcemap=none --outfile=dist/syncengine-serve src/index.ts"
  },
  "dependencies": {
    "@syncengine/core": "workspace:*",
    "@syncengine/gateway-core": "workspace:*",
    "@syncengine/http-core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/node": "^25.6.0",
    "typescript": "~5.9.3"
  }
}
